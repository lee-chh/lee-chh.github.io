// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
// GitHub Pages 사용자 사이트: https://lee-chh.github.io
// 루트(/)에서 서비스되므로 base 설정은 필요 없음.
export default defineConfig({
  site: 'https://lee-chh.github.io',
});
